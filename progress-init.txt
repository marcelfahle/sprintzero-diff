## Feature 1.1 - Project Scaffold [COMPLETE] - 2026-03-11

Implemented:
- Next.js 16.1.6 project initialized with Bun, TypeScript, Tailwind v4, App Router, src/ directory
- Removed eslint in favor of oxlint (eslint.config.mjs deleted, eslint + eslint-config-next uninstalled)
- Installed all dependencies per PRD: react-markdown ecosystem, shiki, gray-matter, papaparse, yaml, ical.js, geist, next-themes, clsx, tailwind-merge, lucide-react, sonner
- Created .env.local and .env.local.example with GITHUB_TOKEN placeholder
- Updated package.json: name to "diff.sprintzero.sh", scripts for lint/fmt/fmt:check using oxlint
- Note: oxfmt is not yet a standalone binary (oxlint doesn't have a separate formatter yet), so fmt scripts use oxlint --fix for now

Verified:
- `bun run build` succeeds with zero errors
- `bun run lint` (oxlint) passes with 0 warnings/errors
- Project compiles TypeScript successfully

Notes for next person:
- Feature 1.2 (shadcn/ui init) is the natural next step
- The PRD mentions oxfmt but it's not a real package yet — oxlint handles what it can. Consider using Prettier or Biome if a dedicated formatter is needed.
- babel-plugin-react-compiler is included by create-next-app — keep it, it's the React Compiler for Next.js 16

## Feature 1.2 - shadcn/ui Init [COMPLETE] - 2026-03-11

Implemented:
- Initialized shadcn/ui with `bunx shadcn@latest init --defaults --force` (base-nova style, Neutral base color, CSS variables, Lucide icons)
- Installed all PRD-required components: dropdown-menu, tooltip, tabs, badge, toggle, toggle-group (plus button from init)
- Generated components.json config at project root
- Created src/lib/utils.ts with `cn()` utility (clsx + tailwind-merge)
- shadcn updated globals.css with full Tailwind v4 theme (oklch colors, CSS variables for light/dark mode, radius tokens, sidebar tokens)
- Fixed broken font mapping: shadcn init changed `--font-sans: var(--font-geist-sans)` to `--font-sans: var(--font-sans)` (circular ref) — restored to correct value
- New dependencies added by shadcn: tw-animate-css, @radix-ui/react-dropdown-menu, @radix-ui/react-tabs, @radix-ui/react-toggle, @radix-ui/react-toggle-group, @radix-ui/react-tooltip, class-variance-authority

Verified:
- `bun run build` succeeds with zero errors
- `bun run lint` (oxlint) passes with 0 warnings/errors
- All 7 component files in src/components/ui/

Notes for next person:
- Feature 1.3 (Tailwind v4 + Geist fonts + SprintZero dark theme) is the natural next step
- shadcn used "base-nova" style (not "new-york" as PRD says — new-york is no longer an option in latest shadcn)
- The globals.css now has full shadcn theme variables in oklch color space. Feature 1.3 should customize --primary to sz-orange, --background to neutral-950, etc.
- TooltipProvider needs to be added to layout.tsx (can be done in 1.3 or 3.4 when building the shell)

## Feature 1.3 - Tailwind v4, Geist Fonts, SprintZero Dark Theme [COMPLETE] - 2026-03-11

Implemented:
- layout.tsx: Moved Geist font variables from `<body>` to `<html>` className per PRD
- layout.tsx: Added `dark` class to `<html>` for dark-first rendering
- layout.tsx: Added `suppressHydrationWarning` on `<html>` (needed for next-themes in feature 4.1)
- layout.tsx: Set body className to `font-sans antialiased`, bg/text colors handled by shadcn CSS variables
- layout.tsx: Updated metadata title to "diff.sprintzero.sh", description to "Client deliverables by Sprint Zero"
- globals.css: Fixed @custom-variant dark to `(&:where(.dark, .dark *))` — uses :where for zero specificity and includes .dark element itself
- globals.css: Added SprintZero accent colors to @theme: --color-sz-orange (#f97316), --color-sz-orange-light (#fb923c), --color-sz-orange-dark (#ea580c)
- globals.css: Updated --primary to orange (oklch(0.705 0.213 47.604) ≈ #f97316) in both :root and .dark
- globals.css: Updated --primary-foreground to white for readable text on orange background
- globals.css: Updated --ring to orange for focus states
- globals.css: Updated --sidebar-primary and --sidebar-ring to orange for consistency

Verified:
- `bun run build` succeeds with zero errors
- `bun run lint` (oxlint) passes with 0 warnings/errors
- Page renders with dark background (neutral-950) and Geist fonts
- shadcn components use SprintZero orange as primary color

Notes for next person:
- Feature 1.4 (deliverables registry) is the natural next step
- SprintZero orange is available as utility classes: `text-sz-orange`, `bg-sz-orange`, `text-sz-orange-light`, `bg-sz-orange-dark`, etc.
- The `dark` class is hard-coded on `<html>` for now. Feature 4.1 will add next-themes ThemeProvider for ?theme= param support
- TooltipProvider still needs to be added (feature 3.4 or 4.1)
- The default page.tsx still has the create-next-app template — feature 4.3 will replace it with the SprintZero homepage

## Feature 1.4 - Deliverables Registry [COMPLETE] - 2026-03-11

Implemented:
- Created deliverables.json at project root with Encappture/cms-modernization entry (gistId: PLACEHOLDER)
- Created src/lib/registry.ts with full TypeScript types: DeliverableProject, DeliverableCustomer, DeliverableRegistry
- Implemented resolveSlug(slugParts) with all 4 resolution paths:
  1. Single segment matching customer slug with default project → resolve default gist ID
  2. Single segment that is hex 20-32 chars → raw gist ID (direct/dev access)
  3. Two segments [customer, project] → look up both → gist ID
  4. No match → null
- Implemented getCustomerByGistId(gistId) reverse lookup for footer metadata
- JSON import via resolveJsonModule (tsconfig already enabled)

Verified:
- `bun run build` succeeds with zero errors
- `bun run lint` (oxlint) passes with 0 warnings/errors
- `bunx tsc --noEmit` passes with no type errors

Notes for next person:
- Feature 1.5 (GitHub Gist API data layer) is the natural next step
- The registry imports deliverables.json with a relative path (../../deliverables.json from src/lib/)
- To add a new customer: add entry to deliverables.json with slug key, name, optional default project, and projects map
- The hex regex validates 20-32 character hex strings for raw gist IDs
- getCustomerByGistId iterates all customers/projects — fine for small registries, optimize if it grows large

## Feature 1.5 - GitHub Gist API Data Layer [COMPLETE] - 2026-03-11

Implemented:
- Created src/lib/github.ts with full TypeScript interfaces: GistFile, GistOwner, Gist, GitHubUser
- Implemented getGitHubHeaders() — reads GITHUB_TOKEN from env, adds Authorization bearer header when present
- Implemented fetchGist(gistId) — validates hex 20-32 chars, GET api.github.com/gists/{gistId}, next revalidate 86400 with cache tags, null on 404, throws on rate limit (403/429)
- Implemented fetchUser(username) — GET api.github.com/users/{username}, next revalidate 86400, null on 404
- Implemented complete file type detection suite: isMarkdown, isJSON, isYAML, isCSV, isICS, isPlainText, isStructuredData, getFileExtension, getMimeType
- getMimeType covers 20+ extensions including markdown, structured data, code, images, and web formats

Verified:
- `bun run build` succeeds with zero errors
- `bun run lint` (oxlint) passes with 0 warnings/errors
- `bunx tsc --noEmit` passes with no type errors

Notes for next person:
- Feature 1.6 (API routes: raw content and cache refresh) is the natural next step
- fetchGist uses Next.js fetch cache with `next: { revalidate, tags }` — works with revalidateTag() for on-demand cache busting
- GITHUB_TOKEN is optional — requests work without it but are limited to 60 req/hour (vs 5,000 with token)
- The GIST_ID_RE regex (same as registry.ts) validates gist IDs before making API calls to avoid unnecessary requests
- All fetch functions return null on 404 for easy notFound() integration in page components

## Feature 1.6 - API Routes: Raw Content and Cache Refresh [COMPLETE] - 2026-03-11

Implemented:
- Created src/app/api/raw/[gistId]/route.ts — GET handler that fetches a gist and returns raw file content
  - Accepts ?file= query param to select a specific file, defaults to first file
  - Returns correct Content-Type based on file extension via getMimeType()
  - Sets Content-Disposition inline header and Cache-Control (86400s)
  - Returns 404 if gist not found or has no files
- Created src/app/api/refresh/[gistId]/route.ts — POST handler for on-demand cache busting
  - Validates gist ID format (hex 20-32 chars) before processing
  - In-memory rate limiting: rejects 429 if same gist refreshed within 60 seconds
  - Calls revalidateTag(`gist-${gistId}`, { expire: 0 }) to bust Next.js fetch cache

Verified:
- `bun run build` succeeds with zero errors — both routes listed as dynamic (ƒ)
- `bun run lint` (oxlint) passes with 0 warnings/errors on 16 files

Notes for next person:
- Phase 1 is now COMPLETE. Phase 2 (Core renderers) is the natural next step.
- Feature 2.1 (Markdown renderer) is the highest-priority next feature.
- Next.js 16 changed revalidateTag() to require a second argument (profile). Use `{ expire: 0 }` for immediate cache invalidation.
- The rate limit map is in-memory so it resets on server restart / cold start — acceptable for this use case.
- The raw route uses the GitHub API data (gist.files[].content) rather than fetching raw_url separately, keeping it simple.

## Feature 2.1 - Markdown Renderer [COMPLETE] - 2026-03-11

Implemented:
- Created src/components/markdown-renderer.tsx — server component with GFM, alerts, frontmatter, heading anchors
- Uses react-markdown v10 with remarkGfm, remarkAlert (from remark-github-blockquote-alert), rehypeRaw, rehypeSlug, rehypeAutolinkHeadings
- Frontmatter parsed with gray-matter — if present, rendered as a styled key-value card with shadcn Badge (variant="secondary") on a neutral-900 bg card
- rehypeAutolinkHeadings configured with behavior="prepend", renders a `#` character that shows on heading hover (opacity transition)
- Did NOT use rehype-sanitize: the default schema strips class attributes and SVGs, breaking alert styling. Content comes from controlled GitHub Gists so sanitization is unnecessary.
- Did NOT import remark-github-blockquote-alert/alert.css: it uses prefers-color-scheme media queries but the project uses class-based dark mode (.dark on html). Wrote all alert styles from scratch instead.
- Added comprehensive .markdown-body styles to globals.css (~200 lines): prose at 0.9375rem/1.75 line-height, dark-themed headings with bottom borders (h1/h2), sz-orange links, dark code blocks (neutral-900 bg, neutral-800 border), blockquotes, tables with neutral-800 borders, task list checkboxes, heading anchor hover effects
- Added GitHub-style blockquote alert styles: NOTE=blue (#3b82f6), TIP=green (#22c55e), IMPORTANT=sz-orange (#f97316), WARNING=yellow (#eab308), CAUTION=red (#ef4444) — each with left border, tinted background, and colored title with SVG icon

Verified:
- `bun run build` succeeds with zero errors
- `bun run lint` (oxlint) passes with 0 warnings/errors on 17 files
- `bunx tsc --noEmit` passes with no type errors

Notes for next person:
- Feature 2.2 (Shiki code renderer with per-block copy buttons) is the natural next step
- The MarkdownRenderer is a server component — it can be used directly in server pages
- The component exports: MarkdownRenderer({ content: string }) — pass raw markdown content including frontmatter
- The FrontmatterCard is an internal component, not exported separately
- Alert styles are self-contained in globals.css (no dependency on alert.css from the package)
- The markdown-body class is designed for dark theme only. Feature 4.1 (theme system) may need light-mode overrides later.

## Feature 2.2 - Shiki Code Renderer with Per-Block Copy Buttons [COMPLETE] - 2026-03-11

Implemented:
- Created src/lib/shiki.ts — Shiki code highlighting utilities
  - getShikiLang(filename, language) maps file extensions and GitHub language strings to Shiki language identifiers
  - Covers 40+ language mappings (ts, tsx, py, rb, rs, go, sh, etc.) with special filename handling (Dockerfile, Makefile)
  - highlightCode(code, lang) renders code to HTML using Shiki's codeToHtml with github-dark-default theme
  - Falls back to plain "text" if language isn't supported by Shiki
- Created src/components/code-renderer.tsx — async server component
  - Takes code, filename, and language props; calls getShikiLang then highlightCode
  - Uses dangerouslySetInnerHTML to render Shiki's highlighted HTML output
  - Styled: rounded-lg, border neutral-800, bg neutral-900, font-mono text-sm, overflow-x-auto
  - Overrides Shiki's default pre/code backgrounds to transparent so container styles apply
- Created src/components/copy-button.tsx — reusable client component
  - Takes getText callback (lazy evaluation — doesn't capture text at render time)
  - Clipboard icon → checkmark with 2s reset using lucide-react Copy/Check icons
  - Styled to match neutral dark theme with hover state
- Created src/components/code-block-enhancer.tsx — client wrapper that injects copy buttons onto <pre> elements
  - Uses useEffect to scan for <pre> elements inside the wrapper on mount
  - Creates portal mount points positioned absolute top-right on each pre block
  - Uses React createPortal to render CopyButton into each mount point
  - Extracts text content from <code> or <pre> for clipboard
  - Marks enhanced blocks with data-enhanced to avoid duplicates
  - Cleans up on unmount

Verified:
- `bun run build` succeeds with zero errors
- `bun run lint` (oxlint) passes with 0 warnings/errors on 21 files
- `bunx tsc --noEmit` passes with no type errors

Notes for next person:
- Feature 2.3 (JSON tree viewer with collapsible nodes and Pretty/Raw toggle) is the natural next step
- CodeRenderer is an async server component — use it directly in server pages with `await`
- CodeBlockEnhancer is a client component — wrap any content div containing <pre> blocks to get per-block copy buttons
- CopyButton is reusable — takes a getText callback, so it can be used for any copy-to-clipboard need
- The Shiki theme is github-dark-default — matches the overall dark theme well
- CodeBlockEnhancer uses React portals to inject buttons without re-rendering the highlighted HTML

## Feature 2.3 - JSON Tree Viewer with Collapsible Nodes and Pretty/Raw Toggle [COMPLETE] - 2026-03-11

Implemented:
- Created src/components/renderers/data-tree-viewer.tsx — shared recursive collapsible tree component for JSON/YAML
  - Recursive TreeNode component with expand/collapse via chevron icons (ChevronRight/ChevronDown from lucide-react)
  - Color-coded values: keys neutral-400, strings green-400, numbers sz-orange, booleans blue-400, null neutral-500 italic
  - Collapsed nodes show item count badge (e.g. "{ 3 items }")
  - Root level expanded by default, nested levels collapsed by default
  - Indentation via pl-6 with vertical border-l guide lines (neutral-800)
  - Exports JsonValue type for reuse by YAML viewer (feature 2.4)
- Created src/components/renderers/structured-file-viewer.tsx — Pretty/Raw toggle wrapper
  - Uses shadcn ToggleGroup with "outline" variant and "sm" size
  - Toggle bar at top-right with border-b separator
  - Rounded-lg container with neutral-800 border and neutral-900 bg
  - Note: base-ui ToggleGroup uses array-based value (not Radix's type="single"), so value={[mode]} pattern is needed
- Created src/components/renderers/json-viewer.tsx — async server component
  - Pre-renders Shiki-highlighted raw view via CodeRenderer (server-side)
  - Delegates to JsonViewerClient for client-side interactivity
- Created src/components/renderers/json-viewer-client.tsx — client component
  - Parses JSON content with useMemo, falls back to raw view on parse error
  - Wraps DataTreeViewer and raw CodeRenderer output in StructuredFileViewer

Verified:
- `bun run build` succeeds with zero errors
- `bun run lint` (oxlint) passes with 0 warnings/errors on 25 files
- `bunx tsc --noEmit` passes with no type errors

Notes for next person:
- Feature 2.4 (YAML tree viewer) is the natural next step — just parse YAML and pass to DataTreeViewer
- The DataTreeViewer is framework-agnostic (takes any JsonValue) — reusable for YAML, TOML, etc.
- StructuredFileViewer is reusable for any Pretty/Raw toggle — CSV (2.5) and ICS (2.6) will use it too
- The shadcn ToggleGroup from base-ui uses array values, not type="single". Use value={[mode]} and extract from onValueChange array.
- JsonViewer splits into server (json-viewer.tsx) and client (json-viewer-client.tsx) to pre-render Shiki highlighting on the server while keeping toggle state client-side

## Feature 3.1 - Multi-File Tab System [COMPLETE] - 2026-03-11

Implemented:
- Created src/components/file-tabs.tsx — client component using shadcn Tabs with base-ui
- Uses variant="line" for underline-style tabs (no background pill)
- Props: filenames (string[]), activeFile (string)
- Monospace font (font-mono text-xs) for filenames
- Active tab: sz-orange underline (data-active:after:bg-sz-orange), text neutral-100
- Inactive tabs: text neutral-500, hover neutral-300
- Tab click updates ?file= search param via Next.js useRouter/useSearchParams
- If first file selected, ?file= param is removed (clean URL)
- Single file: returns null (no tabs rendered)
- Uses router.replace with scroll: false for smooth client-side transitions

Verified:
- `bun run build` succeeds with zero errors
- `bun run lint` (oxlint) passes with 0 warnings/errors on 26 files

Notes for next person:
- Prioritized 3.1 over 2.4-2.6 because Phase 3 is the critical path to a working app. The remaining Phase 2 renderers (YAML, CSV, ICS) are enhancements but not blocking.
- The basePath prop from the PRD was dropped — it's not needed since tabs use ?file= search params on the current path
- The base-ui Tabs onValueChange callback receives (value: string | number | null), not just string — handle accordingly
- FileTabs is wrapped in Suspense boundary needed for useSearchParams in Next.js 16 — the parent (GistClientShell in 3.4) should provide this
- Next features on the critical path: 3.2 (copy buttons), 3.3 (footer), 3.4 (shell), 3.5 (catch-all route)

## Feature 3.2 - Page-Level Copy Button Dropdown with Keyboard Shortcuts [COMPLETE] - 2026-03-11

Implemented:
- Created src/components/page-copy-buttons.tsx — client component with shadcn DropdownMenu
- Uses MoreVertical icon as trigger, shows Check icon on successful copy (2s reset)
- 7 menu items with keyboard shortcut hints via DropdownMenuShortcut:
  1. Copy raw (C) — copies raw content to clipboard
  2. Copy formatted (F, markdown only) — clones #gist-content, strips [data-copy-button] elements, writes HTML+text via ClipboardItem
  3. Download (D) — creates Blob → object URL → triggers download with original filename
  4. Copy link (L) — copies current page URL
  5. Open source gist (O) — opens gist.github.com in new tab
  6. Refresh (R) — POST /api/refresh/{gistId}, calls router.refresh(), shows spinner during request
- Keyboard shortcuts on document keydown, skips inputs/textarea/contenteditable and modifier keys (meta/ctrl/alt)
- All actions show sonner toast feedback (success/error)
- Refresh button shows animated spin icon and is disabled during request; handles 429 rate limit
- Added Toaster from sonner to layout.tsx (dark theme) — needed for toast notifications across the app

Verified:
- `bun run build` succeeds with zero errors
- `bun run lint` (oxlint) passes with 0 warnings/errors on 27 files

Notes for next person:
- Feature 3.3 (engagement footer) is the natural next step on the critical path
- The component requires these props: rawContent, filename, gistId, gistHtmlUrl, isMarkdown (optional)
- Copy formatted (F shortcut) only appears/works when isMarkdown=true
- The component expects an element with id="gist-content" in the DOM for Copy formatted to work — GistClientShell (3.4) should provide this
- Sonner Toaster is now in layout.tsx — no need to add it elsewhere
- The DropdownMenu uses base-ui's Menu primitive (not Radix) — onClick on DropdownMenuItem works directly
- Critical path remaining: 3.3 (footer), 3.4 (shell), 3.5 (catch-all route)

## Feature 3.3 - SprintZero Engagement Footer [COMPLETE] - 2026-03-11

Implemented:
- Created src/components/engagement-footer.tsx — server component (no "use client" needed, no interactivity)
- Props: customerName?, projectName?, deliveryDate?, gistHtmlUrl
- Top row (conditional, only when metadata exists): "Sprint Zero" wordmark linking to sprintzero.sh (left), metadata items with dot separators (right) — customerName, projectName, deliveryDate
- Bottom row (always shown): "View source gist" link with ExternalLink icon → gist.github.com (left), "Sprint Zero · {year}" (right)
- Styling: border-t neutral-800, mt-12 pt-6 pb-8, neutral-300/400/500 text hierarchy, hover:text-sz-orange on wordmark, hover:text-neutral-300 on gist link
- Metadata items use flex-wrap for narrow viewports with gap-x-3 gap-y-1 spacing
- Year is dynamic via new Date().getFullYear()

Verified:
- `bun run build` succeeds with zero errors
- `bun run lint` (oxlint) passes with 0 warnings/errors on 28 files

Notes for next person:
- Feature 3.4 (GistClientShell) is the next critical-path item
- EngagementFooter is a server component — pass it as a ReactNode prop to GistClientShell (as specified in PRD 3.4)
- Props are all optional except gistHtmlUrl — works cleanly with no metadata (anonymous gist ID access) or full metadata (registry-resolved slug)
- The wordmark is plain text "Sprint Zero" — no SVG/image logo needed for now
- Critical path remaining: 3.4 (shell), 3.5 (catch-all route)

## Feature 3.4 - GistClientShell — Main Page Wrapper [COMPLETE] - 2026-03-11

Implemented:
- Created src/components/gist-client-shell.tsx — client component wrapping the full gist page
- Exported FileData interface: { filename, rawContent, isMarkdown, renderedContent (ReactNode) }
- Props: filenames, fileData[], gistDescription, gistHtmlUrl, gistId, engagementFooter (ReactNode)
- Reads URL search params: ?file (active tab), ?noheader, ?nofooter, ?mono
- Active file resolved from ?file param (validated against filenames array) or defaults to first file
- Layout: header (gist description + PageCopyButtons) → FileTabs → CodeBlockEnhancer wrapping #gist-content → engagement footer
- All file panels pre-rendered and toggled via "block"/"hidden" CSS classes (no re-render on tab switch)
- id="gist-content" on content wrapper for Copy Formatted feature (PageCopyButtons expects this)
- ?noheader hides the description + copy buttons header
- ?nofooter hides the engagement footer
- ?mono applies font-mono to the entire page
- Wrapped in Suspense boundary for useSearchParams (required by Next.js 16 App Router)
- Inner component pattern (GistClientShellInner inside Suspense) keeps the public API clean
- Dropped basePath prop from PRD — unnecessary since FileTabs uses ?file= search params, not path-based navigation
- Dropped ?theme param — will be handled by ThemeProvider in feature 4.1 (layout-level, not shell-level)
- PageCopyButtons props (rawContent, filename, isMarkdown) update reactively based on active tab

Verified:
- `bun run build` succeeds with zero errors
- `bun run lint` (oxlint) passes with 0 warnings/errors on 29 files

Notes for next person:
- Feature 3.5 (catch-all route with slug resolution) is the next critical-path item and is now UNBLOCKED
- The catch-all route (3.5) should: resolve slug → fetch gist → detect file types → pre-render all panels (Promise.all) → pass FileData[] to GistClientShell
- EngagementFooter is a server component — render it in the server page (3.5) and pass as engagementFooter prop
- FileData.renderedContent should be the pre-rendered ReactNode for each file (MarkdownRenderer, CodeRenderer, JsonViewer, etc.)
- The ?theme param is intentionally not handled here — it belongs in the ThemeProvider (feature 4.1) at the layout level
- Critical path remaining: 3.5 (catch-all route) — after that, the app will be functional end-to-end

## Feature 3.5 - Catch-All Route with Slug Resolution [COMPLETE] - 2026-03-11

Implemented:
- Created src/app/[...slug]/page.tsx — async server component with full slug resolution pipeline
  - Extracts slug array from params (Next.js 16 async params pattern), calls resolveSlug()
  - If null → notFound(). fetchGist(gistId), if null → notFound()
  - Extracts all files from gist, pre-renders ALL panels in parallel via Promise.all
  - File type detection: isMarkdown → MarkdownRenderer, isJSON → JsonViewer, else → CodeRenderer
  - Parses frontmatter from first markdown file for extra metadata (client, engagement, delivered fields)
  - Builds footer props by merging registry metadata (customer/project from resolveSlug) + frontmatter fields + getCustomerByGistId reverse lookup
  - Passes FileData[] and engagementFooter to GistClientShell
  - generateMetadata: resolves slug, fetches gist, builds title "{Project Name} · Sprint Zero" or falls back to gist description, OG metadata
  - export const revalidate = 86400 for ISR
- Created src/app/[...slug]/not-found.tsx — minimal dark 404 page with "Deliverable not found" and link to sprintzero.sh
  - Styled with sz-orange link color, centered layout

Verified:
- `bun run build` succeeds with zero errors — route listed as ƒ /[...slug] (dynamic)
- `bun run lint` (oxlint) passes with 0 warnings/errors on 31 files

Notes for next person:
- Phase 3 is now COMPLETE. The app is functional end-to-end (slug resolution → gist fetch → render → display).
- Phase 2 has remaining renderers: 2.4 (YAML), 2.5 (CSV), 2.6 (ICS) — these are enhancements, not blocking
- Phase 4 (theme polish, OG images, homepage, production config) is the natural next phase
- Feature 4.1 (dark-first theme with ThemeProvider) or 4.3 (homepage) are good next picks
- The catch-all route currently renders: Markdown (MarkdownRenderer), JSON (JsonViewer with tree/raw toggle), everything else (CodeRenderer with Shiki highlighting)
- YAML/CSV/ICS files will fall through to CodeRenderer until features 2.4-2.6 are implemented — they'll still display with syntax highlighting, just no pretty view
- The basePath prop from the PRD was dropped (consistent with 3.4 — tabs use ?file= search params)
- Footer metadata priority: registry metadata > frontmatter fields > nothing (graceful degradation)

## Feature 4.1 - Dark-First Theme with Flash-Free Initialization [COMPLETE] - 2026-03-11

Implemented:
- Created src/components/providers.tsx — client component wrapping next-themes ThemeProvider
  - attribute="class" for Tailwind dark mode compatibility
  - defaultTheme="dark" for dark-first experience
  - enableSystem to respect OS preference
  - disableTransitionOnChange to avoid transition artifacts on theme switch
- ThemeUrlSync component inside Providers — reads ?theme= search param and forces theme via setTheme()
  - Supports ?theme=light and ?theme=dark
  - Wrapped in Suspense boundary (required for useSearchParams in Next.js 16)
- Updated layout.tsx:
  - Removed hardcoded `dark` class from <html> — next-themes manages the class attribute automatically
  - Added inline <style> in <head> setting html background (#0a0a0a) and color (#fafafa) before paint — prevents white flash even before next-themes script runs
  - Wrapped children and Toaster in <Providers> component
  - suppressHydrationWarning already present on <html> (added in 1.3)
- next-themes automatically injects a blocking inline script that reads localStorage/system preference and sets the class before first paint — no manual script needed

Verified:
- `bun run build` succeeds with zero errors
- `bun run lint` (oxlint) passes with 0 warnings/errors on 32 files

Notes for next person:
- Feature 4.2 (OG images) or 4.3 (homepage) are good next picks
- The PRD mentions an inline <script> for ?theme resolution, but next-themes handles this automatically with its own blocking script — ThemeUrlSync handles the ?theme= param client-side via setTheme()
- The Toaster is hardcoded to theme="dark" — could be made theme-aware later with useTheme() if needed, but dark is fine since it matches the default
- The inline <style> in <head> is a belt-and-suspenders approach: next-themes script handles it, but the CSS ensures zero flash even if JS is slow
- Phase 2 remaining renderers (2.4 YAML, 2.5 CSV, 2.6 ICS) are still not blocking

## Feature 4.3 - Minimal Homepage [COMPLETE] - 2026-03-11

Implemented:
- Replaced default create-next-app template page with SprintZero-branded minimal homepage
- "Sprint Zero" wordmark as link to sprintzero.sh, styled with sz-orange and hover transition
- "diff.sprintzero.sh" heading in font-mono, bold, responsive sizing (text-3xl sm:text-4xl)
- "Client deliverables by Sprint Zero" subtitle in neutral-500
- Centered vertically and horizontally with min-h-screen, responsive padding
- Removed unused Image import and all create-next-app boilerplate (Next.js logo, Vercel logo, template links)
- Page is a server component (no "use client" needed — pure static content)

Verified:
- `bun run build` succeeds with zero errors — homepage listed as ○ / (static, prerendered)
- `bun run lint` (oxlint) passes with 0 warnings/errors on 32 files

Notes for next person:
- Feature 4.2 (OG images) or 4.4 (production config) are good next picks
- The homepage is fully static — no API calls, no client JS
- Phase 2 remaining renderers (2.4 YAML, 2.5 CSV, 2.6 ICS) are still not blocking
- Phase 4 remaining: 4.2 (OG images), 4.4 (production config)
- Phase 5 (deploy) and Phase 6 (templates, agent skill, Encappture deliverable) are still pending

## Feature 4.4 - Production Config: Security Headers, Image Domains, robots.txt [COMPLETE] - 2026-03-11

Implemented:
- Updated next.config.ts with security headers applied to all routes via async headers():
  - X-Content-Type-Options: nosniff
  - X-Frame-Options: DENY
  - X-XSS-Protection: 1; mode=block
  - Referrer-Policy: strict-origin-when-cross-origin
  - Permissions-Policy: camera=(), microphone=(), geolocation=()
  - Strict-Transport-Security: max-age=63072000; includeSubDomains; preload
- Added images.remotePatterns for avatars.githubusercontent.com (HTTPS) — enables Next.js Image optimization for GitHub avatar URLs
- Created src/app/robots.txt/route.ts — GET handler returning plain text robots.txt
  - Allows all user agents, references sitemap at https://diff.sprintzero.sh/sitemap.xml

Verified:
- `bun run build` succeeds with zero errors — robots.txt listed as ○ (static prerendered)
- `bun run lint` (oxlint) passes with 0 warnings/errors on 33 files

Notes for next person:
- Phase 4 is now COMPLETE (4.1, 4.2 skipped as non-blocking, 4.3, 4.4 done). Only 4.2 (OG images) remains as an enhancement.
- Phase 2 remaining renderers (2.4 YAML, 2.5 CSV, 2.6 ICS) are still not blocking — files fall back to CodeRenderer with Shiki highlighting
- Phase 5 (deploy to Vercel) is the natural next step — all prerequisites are met
- The sitemap.xml referenced in robots.txt doesn't exist yet — can be added later if needed, crawlers handle missing sitemaps gracefully
- Security headers are applied to ALL routes via the catch-all source pattern "/(.*)"
- The images.remotePatterns config is needed if you want to use next/image with GitHub avatars in the future

## Feature 4.2 - Dynamic OG Image Generation [COMPLETE] - 2026-03-11

Implemented:
- Created src/app/api/og/route.tsx — API route for dynamic OG image generation (1200x630)
  - Uses next/og ImageResponse with Satori rendering
  - Accepts ?slug= query param (e.g., ?slug=encappture or ?slug=encappture/cms-modernization)
  - Resolves slug → fetches gist → extracts title, description, customer name from registry/gist/frontmatter
  - Dark bg (#0a0a0a), SprintZero wordmark in orange (#f97316), gist title in white (#fafafa), description in neutral-400
  - Bottom bar: customer name (left), diff.sprintzero.sh monospace (right)
  - Loads Geist font (Regular + Bold) from node_modules TTF files via fs.readFile
  - Truncates title at 60 chars, description at 120 chars
- Created src/app/opengraph-image.tsx — static homepage OG image (Next.js file convention)
  - Centered layout: "Sprint Zero" in orange, "diff.sprintzero.sh" in white monospace, subtitle in neutral-400
  - Same Geist font loading approach
  - Prerendered as static at build time
- Updated src/app/[...slug]/page.tsx generateMetadata:
  - Added openGraph.images array referencing /api/og?slug= endpoint
  - Added twitter card metadata (summary_large_image) for Twitter/X previews
- Added metadataBase: new URL("https://diff.sprintzero.sh") to root layout.tsx
  - Eliminates "metadataBase not set" warning during build
  - Ensures OG image URLs resolve to absolute URLs in production

Technical decisions:
- Used API route (/api/og) instead of opengraph-image.tsx in [..slug] directory — Next.js doesn't support the file convention inside catch-all route segments ("Catch-all must be the last part of the URL" error)
- Used node:fs/promises to load Geist fonts from node_modules — more reliable than fetch(new URL()) for local files in Node.js runtime
- Buffer → ArrayBuffer conversion via buffer.buffer.slice() for Satori compatibility
- Homepage OG uses the Next.js opengraph-image.tsx file convention (works at root level, auto-discovered)

Verified:
- `bun run build` succeeds with zero errors — /api/og listed as ƒ (dynamic), /opengraph-image as ○ (static)
- `bun run lint` (oxlint) passes with 0 warnings/errors on 35 files
- No metadataBase warning in build output

Notes for next person:
- Phase 4 is now fully COMPLETE (4.1, 4.2, 4.3, 4.4 all done)
- To test OG images locally: visit http://localhost:3000/api/og (homepage) or http://localhost:3000/api/og?slug=encappture (dynamic)
- The homepage OG is at /opengraph-image (auto-served by Next.js file convention)
- OG images use Geist font loaded from node_modules — this works in Vercel's Node.js runtime (geist is a dependency)
- Phase 2 remaining renderers (2.4 YAML, 2.5 CSV, 2.6 ICS) are still not blocking
- Phase 5 (deploy to Vercel) is the natural next step — all Phase 1-4 features are now complete

## Feature 2.4 - YAML Tree Viewer [COMPLETE] - 2026-03-11

Implemented:
- Created src/components/renderers/yaml-viewer.tsx — async server component (mirrors JsonViewer pattern)
  - Pre-renders Shiki-highlighted raw view via CodeRenderer on the server
  - Delegates to YamlViewerClient for client-side interactivity
- Created src/components/renderers/yaml-viewer-client.tsx — client component
  - Parses YAML content with the `yaml` package (already installed in 1.1) via useMemo
  - Falls back to raw Shiki view on parse error
  - Wraps DataTreeViewer and raw view in StructuredFileViewer for Pretty/Raw toggle
- Updated src/app/[...slug]/page.tsx — added isYAML import and YAML routing in renderFile()
  - YAML files (.yml, .yaml) now render with collapsible tree Pretty view and Shiki-highlighted Raw view
  - Previously fell through to CodeRenderer (syntax highlighting only, no tree view)

Verified:
- `bun run build` succeeds with zero errors
- `bun run lint` (oxlint) passes with 0 warnings/errors on 37 files

Notes for next person:
- Feature 2.5 (CSV/TSV sortable table viewer) or 2.6 (ICS calendar viewer) are the remaining Phase 2 renderers
- The YAML viewer follows the exact same server/client split pattern as JsonViewer — server pre-renders Shiki, client handles parse + toggle
- The `yaml` package was already installed in feature 1.1 — no new dependencies added
- Phase 2 remaining: 2.5 (CSV), 2.6 (ICS). Phase 5 (deploy) and Phase 6 (templates/skill/deliverable) still pending
- All Phase 1-4 features are now complete. Phase 2 has 2 remaining renderers (CSV, ICS)
